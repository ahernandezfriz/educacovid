let num_segments,randomQuestions=[],abcCompleto=!1,nComas;function leerArchivo(e){var archivo=e.target.files[0];if(archivo){var lector=new FileReader;lector.onload=function(e){var contenido=e.target.result;if(contenido){var lineas=contenido.split("\n");lineas.pop(),cadena=lineas[0];for(var indices=[],i=0;i<cadena.length;i++)","===cadena[i].toLowerCase()&&indices.push(i);for(var linea of(nComas=indices.length,num_segments=lineas.length,abcCompleto=26==num_segments,lineas))letra=linea.split(",").map(item=>item.trim()),randomQuestions.push({letter:letra[0],answer:letra[1],status:0,question:letra[2]})}},lector.readAsText(archivo),console.log("Es un "+typeof randomQuestions),console.log(randomQuestions)}}fileInput=document.getElementById("file-input"),fileInput.addEventListener("change",leerArchivo,!1);