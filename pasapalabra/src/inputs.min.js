window.randomQuestions=[];let abcCompleto=!0,num_segments=0;document.addEventListener("DOMContentLoaded",(function(){const letters="abcdefghijklmnopqrstuvwxyz".split(""),lettersContainer=document.getElementById("lettersContainer");function loadSavedData(){const savedData=localStorage.getItem("roscoGameData");if(savedData){const parsedData=JSON.parse(savedData);parsedData.forEach(item=>{item.letter&&(document.getElementById(`word-${item.letter}`).value=item.answer||"",document.getElementById(`question-${item.letter}`).value=item.question||"")}),console.log("Datos cargados desde localStorage.")}}letters.forEach(letter=>{const row=document.createElement("div");row.className="letter-row",row.innerHTML=`\n            <div class="letter-label">${letter}</div>\n            <input type="text" class="word-input" id="word-${letter}" placeholder="Palabra" />\n            <textarea class="question-input" id="question-${letter}" placeholder="Pregunta"></textarea>\n        `,lettersContainer.appendChild(row)}),loadSavedData()}));